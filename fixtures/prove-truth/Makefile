# Truth fixture for the prove loop (proposal §17.3): a deterministic
# workload whose test depends on state created by provisioning, so the
# snapshot-fork contract is exercised for real.

deps:
	sh scripts/provision.sh

test:
	sh scripts/check.sh
